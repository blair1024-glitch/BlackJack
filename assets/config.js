/* 起手式 — 本機設定
   這是唯一需要手動編輯的檔案。 */
window.CONFIG = {

  /* 盤中報價代理的網址。留空就不抓報價，網站其他功能照常運作。
     部署方式看 worker/quote-proxy.js 開頭的說明，Cloudflare 免費方案就夠。
     例："https://tw-quote.你的帳號.workers.dev" */
  quoteProxy: "",

  /* 幾秒重抓一次。證交所那個端點是給人看的，不要調到個位數。 */
  refreshSeconds: 15,

  /* 盤中時段（台北時間）。非交易時段不輪詢，省得白打。 */
  marketOpen: "09:00",
  marketClose: "13:35",

  /* 自用模式。
     ⚠️ 打開之前請先確認這個 repo 是 private、或 GitHub Pages 已經關掉。
        公開的網站對不特定人提供個股買賣判斷，在台灣屬於證券投資顧問業務。

     打開之後改變的只有「措辭」：
       - 拿掉為了公開發布而寫的法律說明，講法更直接
       - 觀察名單從「篩選結果不是推薦」改成「今天通過你設的條件的」
     不會改變的東西（那些是工程品質，跟給誰看無關）：
       - 硬紅旗照樣擋
       - 資料日期與來源照樣標
       - 「API 查不到要自己查」的清單照樣列
       - 報價照樣標明不是逐筆即時 */
  selfUse: false
};
